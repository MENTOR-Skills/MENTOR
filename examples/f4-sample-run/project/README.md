# toy-train（F4 夹具课题）

最小假项目：演示「有训练脚本 + 一次 run 日志」，供 `progress-digest` 做阶段总结。

**不是**真实实验结果。

## 结构

- `train.py` — 占位训练入口
- `runs/exp001/train.log` — 含若干指标行
- `runs/exp001/metrics.csv` — 可核对数字

## 本周目标（虚构）

跑通 baseline 训练循环，记录 val_acc；尚未做消融。
