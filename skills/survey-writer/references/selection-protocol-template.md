# selection-protocol 模板（系统梳理 / 映射 · 过程文件）

> 交付路径：`_work/selection-protocol.md`  
> 仅在选择 **系统梳理/映射** 写法包时**必填**；其它写法包不需要。

```markdown
# 选文与映射协议：<课题简称>

- 关联交付：`survey.md`
- 范围文档：`scope.md`
- 文献库：`references.json`
- 最后更新：<日期>

## 1. Review 问题

一句话说明要回答什么（例如：「2017–2024 间序列建模中自注意力变体如何按训练范式分类？」）。

## 2. 检索来源

| 来源 | 检索式 / 关键词 | 时间窗 | 备注 |
|------|-----------------|--------|------|
| OpenAlex | … | … | 实际执行日期 |
| arXiv | … | … | 可选 |
| 用户补 PDF | — | — | 若有 |

未执行的来源不要写「已检索」。

## 3. 纳入标准

- …

## 4. 排除标准

- …

## 5. 筛选流程（PRISMA-lite）

| 阶段 | 数量 | 说明 |
|------|------|------|
| 检索命中 | ? | 来自检索日志或 `_work/` 中间文件 |
| 去重后 | ? | |
| 标题摘要筛选后 | ? | |
| 全文评估后纳入 | ? | 应 ≈ `references.json` 纳入篇数 |

**若某阶段无精确数字，写「未统计」并说明原因，禁止编造。**

## 6. 映射维度与编码规则

| 维度 | 定义 | 编码取值示例 |
|------|------|--------------|
| 方法族 | … | … |
| 证据类型 | … | … |

编码须能回溯到每篇的 `reading_notes` 或 `id`。

## 7. 映射表（库内文献）

| id / 短引 | 维度 A | 维度 B | … | 备注 |
|-----------|--------|--------|---|------|
| … | … | … | … | abstract_only 等 |

## 8. 局限与偏倚

- 检索库覆盖、语言、预印本比例、摘要级阅读等。

## 9. 变更记录

| 日期 | 变更 |
|------|------|
| … | 初稿 |
```

### 与正文的关系

- `survey.md` §2 可摘要本文件；细节以本 protocol 为准。
- 纳入篇目变更时：先改 `references.json` 与 protocol，再改正文。
