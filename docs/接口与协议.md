# 接口与协议

> 协议正文已齐：目录与交付名、`references.json` 最小字段、`cite_key` 策略、`SKILL.md` 章节、工作深浅（用户可选）、调用关系初版、完成定义（DoD）。

---

## 1. 仓库内目录职责（技能包）

```text
MENTOR/
├── README.md
├── 融合设计方案.md
├── docs/                    # 协作与协议文档
├── skills/                  # 可安装技能（每个子目录一个技能）
├── shared/                  # 全组共用说明（诚信清单、目录说明等）
├── domain-packs/            # 可选：按研究方向细化的内容包
│   └── _template/           # 新建领域包时的模板目录
└── examples/                # 演示用样例（假课题、话术等）
    └── sample-topic/
```

| 路径 | 谁维护 | 说明 |
|------|--------|------|
| `skills/` | 各功能负责人 | 只放技能定义与脚本，不放用户论文全文 |
| `shared/` | 横切负责人牵头 | 多技能共用的 Markdown 说明，避免各写一份漂移 |
| `domain-packs/` | F1 牵头 | 通用入门技能读这里的内容；无包时仍可对话生成 |
| `examples/` | 任意人可补 | 联调样例；大 PDF 默认不进库（见 `.gitignore`） |
| `docs/` | 全员 | 分支、接口、分工等协作文档 |

**不要写入本仓库的内容：** 用户真实课题产出目录、密钥、虚拟环境、模型权重、大批量 PDF。

---

## 2. 用户课题工作区（运行时，默认不进 git）

智能体在某一课题下默认使用（允许用户改根路径）：

```text
campus-research-output/<课题简称>/
├── scope.md
├── findings.md
├── research-log.md
├── _work/
├── pdfs/
│   ├── auto/
│   └── user/
├── references.json
├── reading-reports/
└── …（各功能交付，见下节）
```

本地该目录已被 `.gitignore` 忽略。联调时每人在自己机器上生成即可。

---

## 3. 交付文件名（冻结）

以下名称与《融合设计方案》对齐。**合并功能代码前不得擅自改名**；若要改，先改本文并开文档 PR。

### F1 陌生领域入门路径

- `learning-map.md`
- `prerequisite-gap.md`
- `glossary.md`
- `starter-resources.md`
- `first-practice.md`

### F2 专题文献全景与关系图

- `references.json`
- `literature-landscape.md`
- `reading-shortlist.md`
- `viz/index.html`（关系图网页）
- 可选扩展（用户明确要求综述时）：`survey.md` 和/或 `related-work.md`

### F3 单篇论文深度阅读

- `reading-reports/<论文编号>.md`（`<论文编号>` 与 `references.json` 中该篇的 `id` 一致）
- 结构化信息写入 `references.json`（字段见 §6）
- 复现深度另附：`reproduction-checklist.md`

### F4 本地科研阶段总结

- `artifact-index.md`
- `progress-report.md`
- `result-summary.md`
- `blockers.md`

### F5 基于研究材料的学术写作

**§3 冻结（基本版，仍有效）：**

- `_work/claims-evidence.md`（主张—证据对照，成稿前必须存在）
- `stage-writeup.md`（阶段技术说明，按需）
- `meeting-one-pager.md`
- `qna-prep.md`

**§3 拟新增 · F5 扩大版（已审批；落地技能可用；合并进冻结表前仍以本节「拟新增」列出）：**

文件名一律 **ASCII**（正文可为中文 UTF-8）。详见 [F5设计提纲-扩大版.md](./F5设计提纲-扩大版.md)。

- `meeting-talk.md`
- `meeting-slides.tex` / `meeting-slides-outline.md`（无 LaTeX 时只交 outline）
- `paper-section-<slug>.md`（`<slug>` 为 ASCII，如 `method`）
- `polish-<slug>.md`（`<slug>` 为 ASCII，如 `ieee-access`）
- `tech-doc.md`（或用户指定的 ASCII basename）
- 可选过程文件：`_work/adversarial-notes.md`（`adversarial-lite`；非终稿）

### 横切常见文件

- `scope.md` — 主题、时间范围、用户确认事项、禁止事项等
- `research-log.md` — 追加式工作记录
- `_work/WAITING_*.md` — 正在等待用户完成的事项说明

---

## 4. 单个技能目录形态

```text
skills/<技能名>/
├── SKILL.md          # 必选
├── references/       # 可选：长模板、字段说明
├── assets/           # 可选：渲染模板等静态资源
└── scripts/          # 可选：Python 等程序
```

技能名使用小写英文与连字符（例：`paper-deep-read`）。

---

## 5. 编码与安全底线

1. 中文 Markdown / JSON 使用 UTF-8 保存。  
2. 不得伪造引用、实验数值、下载状态。  
3. 模型推断的论文关系须标明「推断」。  
4. 不得在未授权时扫描用户主目录；忽略密钥与 `.env`。  
5. 不得把付费全文或密钥提交进本仓库。

---

## 6. `references.json` 最小字段

文献库为 JSON 文件；建议顶层为对象，内含 `papers` 数组（或等价列表）。**每一篇论文条目**至少包含下表「必填」列。

| 字段 | 必填 | 含义 |
|------|------|------|
| `id` | 是 | 稳定编号（优先用开放获取库 id、arXiv id；否则团队内手编且全程不变） |
| `title` | 是 | 标题 |
| `authors` | 是 | 作者列表（字符串数组，或统一约定的作者对象数组；同一仓库内保持一种写法） |
| `year` | 是 | 发表年（未知可写 `null`，但字段要有） |
| `venue` | 是 | 出版情况：期刊 / 会议 / 预印本平台等名称；未知写空字符串并在笔记中说明 |
| `url` | 建议 | 可访问链接 |
| `doi` | 建议 | 有则填 |
| `arxiv_id` | 建议 | 有则填 |
| `pdf_path` | 建议 | 本地 PDF 相对路径；尚无全文可空 |
| `access_status` | 是 | 取值建议：`fulltext` / `abstract_only` / `waiting_user_pdf` / `unknown` |
| `abstract` | 建议 | 摘要（中英不限；有英文可另加 `abstract_en`，非强制） |
| `reading_notes` | 深读或入库后必填 | 对象，见下 |

### `reading_notes` 最小内容

| 子字段 | 含义 |
|--------|------|
| `problem` | 研究问题（短句） |
| `method` | 方法（短句） |
| `results` | 主要结果或结论（短句） |
| `limitations` | 局限（短句） |

可选扩展（不强制）：`data_metrics`、`quotable_claims`、证据位置等。

### 关系标注（F2 用，可选）

若记录论文间关系，每条须含：`target_id`、`kind`（如引用 / 方法继承 / 观点冲突等）、`basis`（`fact` 或 `inferred`）。推断不得写成事实。

### `cite_key` 策略（方案 A）

- **不作为入库必填字段**，也不要求本科生手填。  
- 需要写 Related Work、导出文献列表或生成 BibTeX 时，由程序按「第一作者姓（或拼音）+ 年份」等规则**自动生成**短引用键；重名冲突时自动加后缀。  
- 用户侧无感；开发者可在导出结果或 `_work/` 中看到生成值，但不得把「未填 cite_key」当成入库失败原因。

---

## 7. `SKILL.md` 必含章节（已确认）

每个 `SKILL.md` 按下列结构书写（可增不可缺）：

```markdown
---
name: <技能名>
description: >-
  <第三人称：做什么、何时用、关键触发词>
---

# <中文名>

## 何时使用 / 何时不使用
## 前置条件（依赖哪些技能、哪些文件须已存在）
## 硬规则（禁止项）
## 步骤（有序，可勾选）
## 必须问人的点（停下条件与如何继续）
## 交付契约（输入 / 输出路径与字段）
## 脚本调用（若有，给命令模板）
## 失败与回流
## 参考（链接 references/）
```

`description` 写清楚「做什么 + 何时用」，便于 Agent 自动发现；不要写成「我可以帮助你…」。

---

## 8. 技能调用关系（初版，已确认）

```text
调度模块
  → 按用户意图进入 F1 / F2 / F3 / F4 / F5

F2 文献全景（典型顺序）
  检索下载 → 精读入库 → 全景撰写 →（可选）综述撰写 →（可选）引用核对 → 可视化

F3 单篇深读
  → 可调用精读入库能力；结果必须回写同一 references.json

F1 入门
  → 可选调用检索；或本周降级为「内容包 / 用户给定链接」

F4 本地阶段总结
  → progress-digest：确认扫描范围 → 材料索引 → 阶段报告 / 结果摘要 / 阻塞清单
  → 未授权不得扫描；有代码不得断定实验已成功

F5 写作
  → academic-writing：选文体包 → 读取 references.json 与/或 F4 四文件
  → 成稿前必须先有 _work/claims-evidence.md
  → Related Work / 长综述从零写 → F2 survey-writer（不进 F5）
  → 可选 adversarial-lite（标准档不默认；只写 _work/adversarial-notes.md）

学术诚信规程
  → 全程适用，不单独生成科研正文
```

本周允许 F1/F3 在说明书中写明「调用 F2 侧已有技能」，或暂时重复步骤说明并标注后续再抽共享；重组时用 `git mv` 即可。

---

## 9. 工作深浅（用户可选；2026-07-17 修订）

> 修订说明：原「方案 X（对用户透明）」废止。深度档对用户可见、可直接选择；调度也可根据话术推断档位并向用户确认。各档行为上限细则见 `shared/effort-contract.md`。
>
> **现阶段约定（2026-07-17）：基本版本开发期先按「标准」档执行**，轻量 / 深入档暂不开放；下列规则保留，待核心功能稳定后启用。

- 三档：**轻量 / 标准（默认）/ 深入**。
- 用户可直接指定档位；未指定时调度按话术信号推断（见下表），推断不出按**标准**兜底。
- 用户说「随便写写」「快速看看」时，技能可建议降为轻量并请用户确认（话术模板见 `shared/effort-contract.md`）。
- 选定档位写入 `scope.md`；一次任务只用一个档，跨功能组合时各功能可独立设档。

| 档位 | 典型话术信号（推断用） | 行为约束 |
|------|------------------------|----------|
| 轻量 | 「今晚组会」「快点」「先扫一眼」 | 少检索、短交付；必须标明局限；精读候选可自动但标风险 |
| 标准 | 未特别说明（默认） | 完整「必须问人」关卡；主张挂证据 |
| 深入 | 「开题」「正式相关工作」「尽量全面」 | 更广检索；关系图必出；可启用挑刺 |

---

## 10. 完成定义 DoD（已确认）

功能分支合并进 `main` 前，负责人自检：

1. 有一句演示话术（用户说什么 → 应出现哪些文件）  
2. 产出路径与本文 §3 文件名一致  
3. 具备符合 §7 结构的 `SKILL.md`  
4. 不编造证据；推断已标明  
5. 中文交付为 UTF-8  
6. 未提交 PDF 全文、密钥、用户课题大目录  
7. 能在样例材料上当场跑通，或按步骤演示到约定交付  
