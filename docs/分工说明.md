# 分工说明（第一周并行）

> 依据：《融合设计方案.md》、团队约定的功能切分。  
> 原则：启动清单进 `main` 后四条线并行；共用检索/精读允许本周先引用或少量重复，之后再抽成共享技能（可用 `git mv` 重组）。

---

## 1. 角色一览

| 角色 | 负责范围 | 建议分支 |
|------|----------|----------|
| 甲 | **F1** 陌生领域入门路径 | `feat/f1-onboarding` |
| 乙 | **F2** 专题文献全景 + **F4** 本地阶段总结 | `feat/f2-landscape`（F2）；`feat/f4-progress-digest`（F4，可与 F2 同 PR 说明） |
| 丙 | **F3** 单篇论文深度阅读 | `feat/f3-deep-read` |
| 丁 | **横切能力** + **F5** 基于材料的学术写作 | `feat/crosscut-writing` |

请将甲乙丙丁替换为真实姓名后，在本文件或群公告中同步一版。

若其中一人本周支教、需减负：优先让 **丁** 只做「诚信规程 + 主张—证据模板 + 组会一页纸模板」，调度技能写骨架即可；或让支教同学只做 F1 的五类空模板与填写说明（需临时调换角色时改本表）。

---

## 2. 各角色本周交付

### 甲 · F1 入门

| 项 | 内容 |
|----|------|
| 技能 | `skills/domain-onboarding/`（主流程）+ `skills/domain-resource-search/`（3 渠道搜索执行） |
| 流程 | 背景访谈 → `domain-resource-search`（论文/学习资源/开源实践 3 渠道并行）→ 基于搜索结果生成五件套 |
| 交付物 | 能产出或能按模板填写：`learning-map.md` 等五件（见 `docs/接口与协议.md`） |
| 内容包 | 可选：在 `domain-packs/` 放一个极薄示范；无包时不停止，直接搜索 + 对话生成 |
| 依赖 | 论文渠道由 `domain-resource-search` 内部调 F2 脚本（不可用时 Agent 搜索降级） |
| 完成观感 | 给定本科背景与目标方向 → 搜索结果驱动的个性化入门五件套（每条术语可追溯来源） |

### 乙 · F2 文献全景

| 项 | 内容 |
|----|------|
| 起点 | 现有 `demo_skills/literature-survey/`（检索、精读入库、核对、可视化、原综述写作、原总控） |
| 动作 | 迁入或复制到本仓库 `skills/` 后改造；**完成标准改为**全景说明 + 精读清单 + 关系图，不强制长综述 |
| 主交付 | `literature-landscape.md`、`reading-shortlist.md`、`references.json`、`viz/index.html` |
| 可选扩展 | 用户明确要求时：`survey.md` / `related-work.md`（`survey-writer`，四写法包） |
| 重点 | 付费墙须能停下写 `WAITING_*.md`；关系若为推断须标注 |
| 完成观感 | 给定主题 → 有可核对的精读清单与全景说明，关系图数据来自文献库 |

### 乙 · F4 本地阶段总结

| 项 | 内容 |
|----|------|
| 技能 | `skills/progress-digest/`（新建） |
| 主交付 | `artifact-index.md`、`progress-report.md`、`result-summary.md`、`blockers.md` |
| 重点 | 未授权不得扫描；不得因「有训练脚本」断定实验成功；主张挂本地路径或标「口头进展」 |
| 夹具 / 冒烟 | `examples/f4-sample-run/`；`python tests/scripts/run_f4_script_smoke.py` |
| 完成观感 | 给定授权路径 → 四份可核对阶段材料，供 F5 组会读取 |

### 丙 · F3 单篇深读

| 项 | 内容 |
|----|------|
| 技能 | `skills/paper-deep-read/`（独立入口）；精写入库可复用/调用 `literature-reader` |
| 深度 | 快速判断 / 深入分析 / 复现条件 三档（档位细则待协议确认后写死） |
| 主交付 | `reading-reports/<id>.md`；复现档附 `reproduction-checklist.md`；**回写同一 `references.json`** |
| 完成观感 | 给定一篇 PDF → 有人读报告，且文献库中出现对应条目 |

### 丁 · 横切 + F5 写作

> **扩大版已落地（核心骨架，2026-07-18）：** F5 为写作中枢；**唯一写作技能** `academic-writing`（多文体包）+ 可选 `adversarial-lite`；**不单独落地** `meeting-brief`。交付文件名 ASCII；Related Work 禁止 F5 从零写。详见 [F5设计提纲-扩大版.md](./F5设计提纲-扩大版.md)。夹具：`examples/f5-sample-writeup/`。

| 项 | 内容 |
|----|------|
| 横切 | `campus-research-orchestrator`（路由骨架）、`research-workspace` 说明、`academic-honesty`、`AGENTS.md`、`shared/` 共用稿 |
| 写作（核心批） | `skills/academic-writing/`：**可演示（薄）**；强制 `_work/claims-evidence.md`；`meeting-one-pager` / `meeting-qna` / `meeting-talk` / `stage-writeup` |
| 写作（第二批） | PPT / `paper-section` / `polish` / `tech-doc` 薄模板已挂；待补强 |
| 可选 | `adversarial-lite`：**骨架**（只输出 `_work/adversarial-notes.md`，不改终稿；标准档不默认） |
| 完成观感 | 用户「写 / 组会 / PPT / 润色 / 技术文档」等意图路由到 F5 → 选文体；成稿挂本地或文献证据路径 |

---

## 3. 与「完整方案」的关系

本周并行覆盖 **F1、F2、F3、F4、横切、F5（薄）**。  
F4 由乙兼做：`skills/progress-digest/`，分支建议 `feat/f4-progress-digest`，夹具见 `examples/f4-sample-run/`。  
F2 中的检索/可视化等工业能力以「能跑通改造后的完成标准」为准；抽共享模块放到后续重组迭代。

---

## 4. 开发进度（乙 · F2 / F4，截至 2026-07-18）

> 状态口径与 `SKILLS_CATALOG.md` 一致：**未开始 / 骨架 / 开发中 / 可演示 / 可用**。  
> 「可演示」= 技能包 + 夹具 + 脚本冒烟通过；不等于生产级「可用」。

### F2 进度

| 工作项 | 状态 | 说明 |
|--------|------|------|
| 五技能迁入改造（全景主链） | **可演示** | `literature-search-download` / `literature-reader` / `literature-landscape-writer` / `citation-verifier` / `survey-visualizer` |
| 样例夹具 `examples/sample-topic/` | **可演示** | 三篇经典文 + 全景 + 精读清单 + 关系图 |
| 脚本冒烟 | **通过** | `python tests/scripts/run_f2_script_smoke.py` |
| Agent 端到端盲测（全景主链） | **做过一轮** | 含 `tests/runs/page-migration/`（不进 Git） |
| **综述撰写扩展 `survey-writer`** | **可演示（扩展）** | 四写法包 + 模板 + 质量门；夹具 `examples/f2-survey-extension/`；`page-migration` 已跑正式档分类框架综述（本地） |
| 轻量 / 深入档分支行为 | **未做** | 契约已写；开发期统一按标准档 |
| 与 F3 联调回写 | **待丙** | reader 字段已对齐协议 §6 |

**进度说明（F2 与长综述）：** F2 **基本完成标准**仍是「全景 + 精读清单 + 关系图」。`survey-writer` 为**可演示（扩展）**：叙事权威 / 分类框架 / 系统梳理 / Related Work；标准档建议 ≥8 篇有 `reading_notes`、中文约 2500+ 字。演示夹具仅 3 篇须标演示级；`page-migration`（12 篇）已按标准档篇幅成稿（摘要级局限须在正文声明）。

### F4 进度

| 工作项 | 状态 | 说明 |
|--------|------|------|
| `progress-digest` 技能 | **可演示** | SKILL + 四模板 + 列文件 / 路径校验脚本 |
| 夹具 `examples/f4-sample-run/` | **可演示** | 假课题 + `expected/` 四份交付 |
| 脚本冒烟 | **通过** | `python tests/scripts/run_f4_script_smoke.py` |
| Agent 盲测（含无授权负例） | **待做** | 话术见 `tests/README.md` / `examples/f4-sample-run/README.md` |
| 与 F5 读四文件联调 | **夹具已通（薄）** | F5 `examples/f5-sample-writeup/` 挂接本例 expected；Agent 盲测仍待做 |

### 本周开发计划（乙）— 已完成 / 剩余

| 计划项 | 结果 |
|--------|------|
| F2 技能落地 + 协议交付名对齐 | 已完成 |
| F2 examples / tests 基建 | 已完成（`tests/runs/` 默认不提交） |
| F2 `survey-writer` 扩展 | **可演示（扩展）**（四写法包 + 夹具；page-migration 本地成稿） |
| F4 `progress-digest` 落地 | 已完成 |
| F5 `academic-writing` 薄落地 + 提纲 | **可演示（薄）**（见 §4 F5；[F5设计提纲-扩大版.md](./F5设计提纲-扩大版.md)） |
| 开 PR 上传（演示话术 + 路径列表） | 已完成（PR #3 → `main`） |

### 后续方向（乙 / 丁协作，优先三条）

| 方向 | 内容 | 状态 |
|------|------|------|
| **F4 / F5 测试** | F4 Agent 盲测（含无授权负例）；F5 组会核心文体端到端盲测 | **待做** |
| **F2 / F5 写作能力打磨** | `survey-writer` 标准档稳定性；F5 核心文笔与证据挂接，再补第二批文体 | **待做** |
| **可视化审美加强** | `survey-visualizer` 版式与可读性；`meta.audit` 同步展示 | **待做** |

明细短索引见 [开发进度-F2F4.md](./开发进度-F2F4.md)。其余（深度档分支、F3 联调、抽共享技能）不阻塞上表。

---

### F5 进度（丁，截至 2026-07-18）

| 工作项 | 状态 | 说明 |
|--------|------|------|
| `academic-writing` 技能 | **可演示（薄）** | 一体多文体包；核心模板齐；第二批薄模板 |
| `adversarial-lite` | **骨架** | 只出 notes；标准档不默认 |
| 夹具 `examples/f5-sample-writeup/` | **可演示（薄）** | 主张—证据 + 一页纸 + 问答（挂 f4 expected） |
| 独立 `meeting-brief` | **取消** | 组会话术并入文体包 |
| Agent 盲测 / 写作打磨 / 第二批 | **后续方向** | 见上表「F4/F5 测试」「F2/F5 写作打磨」 |

---

## 5. 协作节奏（建议）

| 时间 | 动作 |
|------|------|
| 启动日 | `docs/接口与协议.md` 中「待讨论」五项对齐并补写；骨架已在 `main` |
| 周中 | 乙提供样例 `references.json` 一条；丙按同一字段回写；丁按字段读 |
| 周末前 | 各开 PR；描述中写演示话术与交付路径列表 |

分支规则见 [分支约定.md](./分支约定.md)。

---

## 6. 后续重组（预先约定）

若 F1/F3 与 F2 都用到检索或精读：

1. 本周允许在各自 `SKILL.md` 中写明调用现有技能，或暂时重复步骤说明并标注 `TODO: extract shared`  
2. 接口稳定后，单开 PR 用 `git mv` 抽到共享技能目录，再改各处引用  

此流程在 Git 上可行，不阻塞本周并行。
